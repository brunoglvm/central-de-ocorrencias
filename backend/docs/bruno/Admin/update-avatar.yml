info:
  name: update-avatar
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/me/avatar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
