info:
  name: get-avatar
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/public/condominium"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
